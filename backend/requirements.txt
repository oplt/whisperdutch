# Runtime dependencies for the live subtitle backend.
# For development (tests, lint, types): pip install -r requirements-dev.txt
# For optional WhisperX export: pip install -r requirements-whisperx.txt
fastapi==0.141.1
uvicorn[standard]==0.53.0
numpy==2.4.5
pydantic==2.13.4
pydantic-settings==2.15.0
faster-whisper==1.2.1
ctranslate2==4.8.2
# Transformers v5 is a deliberate migration (API/tokenizer changes). Stay on 4.x until validated.
transformers==4.57.6
sentencepiece==0.2.1
sacremoses==0.1.1
